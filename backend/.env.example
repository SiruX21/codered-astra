# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
DB_HOST=mariadb
DB_PORT=3306
DB_USER=fursonauser
DB_PASSWORD=fursonapass
DB_NAME=fursona_db

# JWT Secret (generate a secure random string)
JWT_SECRET=your_jwt_secret_key_change_this_in_production

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# AI/LLM Configuration
# Use 'gemini' for Google Gemini or 'openai' for OpenAI GPT-4 Vision
LLM_PROVIDER=gemini

# Google Gemini API Key (get from https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key

# OpenAI API (only if using LLM_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key

# Allowed Origins (for CORS)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Subscription Plans (monthly price in cents)
BASIC_PLAN_PRICE_ID=price_basic_monthly
PRO_PLAN_PRICE_ID=price_pro_monthly
